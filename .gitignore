node_modules/
.claude/
.DS_Store
__MACOSX
*.pem
*.log

# Internal project notes — kept locally as working memory, not for publication
CLAUDE.md

# Native Messaging host launcher (per-machine, generated by install
# scripts) — hardcoded absolute path to the local node binary.
chrome-devtools-extension/native-proxy/native-messaging-host.bat
chrome-devtools-extension/native-proxy/native-messaging-host.sh

# Build artifacts
*.zip

# CWS listing screenshots — 1280x800 letterboxed, generated locally from
# docs/screenshots/ source images for Chrome Web Store dashboard upload only.
docs/screenshots/cws/

# CWS reviewer justification — paste-ready submission text, kept local only.
docs/CWS_REVIEW_NOTES.md
